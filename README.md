# Laboratorio 01 & 02 - Tecnologías Emergentes

Este repositorio contiene la configuración de un entorno de desarrollo profesional en Python (entornos virtuales, gestión de dependencias, empaquetado editable con `pyproject.toml` y control de versiones con Git)[cite: 3], así como la implementación de un pipeline de análisis de datos utilizando NumPy, Pandas y la API de Open-Meteo[cite: 1].

---

## Instrucciones de Instalación

1. **Clonar el repositorio:**
   ```bash
   git clone [https://github.com/Vane-UNCP/tecemer-lab1.git](https://github.com/Vane-UNCP/tecemer-lab1.git)
   cd tecemer-lab1

python3 -m venv .venv
source .venv/bin/activate
pip install -e .
